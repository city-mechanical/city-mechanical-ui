import { Meta, Story, Preview } from "@storybook/addon-docs/blocks";
import Head from "next/head";
import CmiMeta from "./meta";

<Meta title={"Components/Html/CmiMeta"} />

# CmiMeta

A component that provides standard meta tags that should be present on all CMI webpages. This component is meant to be included in `_app.js` in all next projects.

<Preview>
  <Story name="Default">
    <Head>
      <CmiMeta />
      <title>(Page Title) - City Mechanical, Inc.</title>
    </Head>
  </Story>
</Preview>
